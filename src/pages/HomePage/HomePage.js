import React from 'react';
import {withRouter} from 'react-router-dom'

// rsc - stateless react component
const HomePage = () => {
    return (
        <div>

        </div>
    );
};

export default withRouter(HomePage);